/**
 * Responsávelrenderização e por manter um padrão de 
 * das mesagens nos placehoder.
 * 
 * ATENÇÃO:
 * Alteração nesse arquivo reflete em todos os componentes que utilizam.
 * 
 * - search: Mensagens de placeholder para campos que realizam filtros.
 */
export const placeholderMessage = {
    search: {
        nameVehicle: `Digite o nome do veiculo...`,
    }
}